# 手动调整手册 ｜ 正式站首页（website/src/pages/index.astro）

> 目的：正式版首页的每个可调值——搜什么字段、大概第几行、怎么改、改了会怎样。
> 行号基于 **2026-09-06 封版版本（v4.2：封版数值 + SCENE_PIN_VH 常量）**，文件再改后行号会漂移，**以"搜索字段"为准**。
> 使用方式：VSCode 打开 `website/src/pages/index.astro` → Ctrl+F 搜字段 → 改数值 → 保存 → 浏览器 **Ctrl+F5**。**一次只改一两个值**，看完效果再继续。改完建议跑一次 `npm run build` 确认无报错。
> 注意：这份手册只管**正式站**；示意稿（mockups/方案A-v3）另有自己的《手动调整手册》，两者数值已分道扬镳，不要混用。
> ⚠️ **英文首页**（2026-09-06 起）：`src/pages/en/index.astro` 是本文件的**翻译副本**（动效/样式同值）。在中文首页调了 pin 场长、位移等数值后，需**手动同步**到 en 副本同名字段，否则英文版动效与中文版不一致。

## 使用前必读（4 条规则）

1. **单位**：`px` 像素；`vh` = 视口高度的 1%（900px 高的屏，100vh=900px）；补间里的 `duration` 不是秒——本站滚动动效是 scrub 模式（跟着滚动进度走），duration 只用来**比比例**；行尾的 `0.45` 这类**位置参数**表示"本场行程的 45% 时刻开始动"（想晚点出现→调大，想早→调小）。
2. **两套 scrub**：`scrub: 0.5` = 动画滞后滚动约半秒平滑追赶（电影感）；`scrub: true` = 1:1 严格即时。位置类锁（如固定层）必须是 true，外观淡入/横移用 0.5。
3. **四条联动红线**（动这几个前先看）：① `.scene { margin-top: -100vh }`（约 603 行）是场间对齐的基准，**不许动**——四块视差窗口、深色底时序、场间交接全依赖它；② GSAP 控制的元素（.blocks/.cats/.rail/.rail-bg/.hero-cluster/.scene-dark-bg 等）**不要再加 CSS transition**（会打架）；③ `html,body` 的 `overflow-x` 必须保持 **clip**（在 `src/styles/global.css`）——改回 hidden 会破坏…目前虽无 sticky，但会改变滚动容器语义，别动；④ **悬停/点击穿透三件套，删任何一半都会"卡片看得见却悬停没反应/点不进"**：`.scene { pointer-events: none }` + 放行组 `.scene a/.cats/.rail/.scene-dark-bg { pointer-events: auto }`（场景全屏层放行输入，深色底作为可见性边界拦被盖区域）+ `.pin-spacer { pointer-events: none }`（GSAP 包装层是全屏定位盒，交接期叠在解钉场景之上，不放过它卡片照样被拦）；`.scene.is-pinned { z-index: 10 }`（pin 期视觉抬层，与三个场景 scrollTrigger 里的 `toggleClass: 'is-pinned'` 配套）。
4. 改动效前先 `git commit` 或记住上一个好版本（`git diff` / `git checkout -- src/pages/index.astro` 可整体回退）。

## 一、换页交接（首屏 → 关于我）

| 内容 | 搜索字段 | 行号 | 当前值 / 怎么改 |
|---|---|---|---|
| **四块下移范围（你问的）** | `y: '2vh'` | 250 | `2vh` = 换页行程中四块相对页面**多滑下 2% 屏高**（滞后感）。调大更"拖曳"（≤10vh，太大出框）；改 `0vh` = 完全跟页刚性划走；负值 = 反向超前 |
| 四块下移的窗口 | `start: 'top top'`（.blocks 那段，253-257） | 253 | 窗口=关于我钉住后的第一个视口高（即首屏 1:1 退场的行程）。`end: () => '+=' + window.innerHeight` 里的 `1` 可改成 `0.8`（下移更快完成）或 `1.2`（更缠绵）。**start 不要改** |
| 首屏 pin 总长 | `end: '+=170%'` | 237 | 170% = 首屏钉住 1.7 屏（含四块入场+大字让位+停留）。调大=首屏更从容 |
| 大字让位幅度 | `y: '-20vh'` | 242 | 首屏钉住期间大字上移 20% 屏高给四块让位 |
| 四块入场幅度/错峰 | `x: -70` / `x: 70` / `stagger: 0.07` | 240-241 | 左右两列滑入距离（70px）与前后错峰 |
| 胶囊引导淡出 | `opacity: 0, duration: 0.5` | 239 | `duration: 0.5` = 占首屏行程的比例，越大消失越慢 |

## 二、三场固定层（关于我 / 分类 / 精选）

| 内容 | 搜索字段 | 行号 | 当前值 / 怎么改 |
|---|---|---|---|
| **每场 pin 总长（含进场+停留）** | `const SCENE_PIN_VH` | 268 定义（274 / 298 / 327 引用） | 1.4 = 场长 140vh（进场一屏 + 停留 0.4 屏）。**唯一入口，pin 三处与导航落点自动同值**——只改 268 行这一个数，别处不用动。调大=整场更从容（入场与停留同比拉长）；调小=更紧凑。**不建议低于 1.2**（停留太短） |
| 入场元素出现的时机 | 各 `.from(...)` 行尾的位置参数（`0.4`/`0.46`/`0.5`/`0.6`…） | 279-291 等 | 每场一组：标题 0.4 → 导语 0.46 → 事实条 0.5 → 奖项 0.6。**想只调"停留"：把最后一个 cue 调小**（如 0.6→0.45），入场提前完成、停留自然变长 |
| 入场滑入距离 | `x: -80` / `x: 90` / `y: 70` | 285/288/291 等 | 各元素从左右/下方滑入的距离（px） |
| 精选深色底升起时长 | `y: '100vh', duration: 0.3` | 333 | 0.3 = 占场长 30%（升满时刻）。配合后续 cue：标题 0.28、底块 0.32、卡片 0.42——**改升起时长时注意别让深色没升满内容就出现** |
| 分类整排滑入 | `duration: 0.42`（.cats 段） | 316 | 整排从画外左侧到位的用时占比；⚠️ 整排是一个补间（容器刚性位移），**别拆成每卡单独动** |
| 精选底块/卡片追及 | `x: '100vw'`（340）/ `duration: 0.42`（348） | 340/348 | 底块从右滑入的距离与时点（0.32 起）；卡片 0.45 起追及 |

## 三、导航与全局

| 内容 | 搜索字段 | 行号 | 当前值 / 怎么改 |
|---|---|---|---|
| 导航点击落点 | `SCENE_PIN_VH * 0.9` | 373 | 落到每场 pin 的 90% 处（停留段中点、内容已完整）。想落到场景刚钉住的位置→改 `0.35`；想更靠后→改 `1.1`（≤1.3，否则快出画）。场长改动自动跟随，无需单独同步 |
| 分类/详情页等内页的手感 | — | — | 内页目前是模板样式 + 模板灰阶映射，可调值在其各自组件；首页手册不管内页 |
| 全站色彩/字体/圆角 | — | — | **不在 index.astro**——单一来源 `docs/网站/02-视觉风格规范.md` §3，落在 `src/styles/global.css` 的 `@theme` 块 |
| rail 横向吸附 | `scroll-snap-type: x proximity` | 808 | proximity=靠近吸附点才吸（防滚轮劫持）；改回 `mandatory` 会恢复强吸附 |

## 五、分类页与作品条（works/[category] + ModelStage，2026-09-06 起）

> 文件：`src/pages/works/[category]/index.astro`（页面/作品条）与 `src/components/ModelStage.astro`（3D Hero）。同样建议小步改 + Ctrl+F5。

| 内容 | 搜索字段 | 怎么改 |
|---|---|---|
| **动画行程总量** | `const ANIM_VH`（ModelStage.astro） | 默认 1 = 听筒动画用一屏滚动播完（1:1） |
| **内容上移时机** | `const CONTENT_ENTER_AT`（ModelStage.astro） | 默认 0.65 = 动画播到 65% 时内容开始上移、与动画尾段同步；1 = 播完才进入；文字层高度自动跟随 |
| **内容层色块宽度**（3D 页滚上来盖住模型的色块） | `.page-body`（分类页） | 默认全幅出血（`width: 100vw; margin-inline: calc(50% - 50vw)`）；内容子项对齐宽度 = `.page-body > *` 的 `max-width: 83rem`（与全局 .wrapper 同值，想改内容宽度改这里） |
| 内容层色块底色（3D 页分界色块） | `.page-body.cover` 的 `background` | 默认 var(--dark)（深色下=#1f2427，比页面底亮一档）；删掉 background = 取消分界色块 |
| 作品条高度 / ✕ 位置 | `.sheet` 的 `height` / `.sheet-close` 的 `top/right` | 默认 93vh；✕ 在条内右上 (16,16) |
| 实机窗比例（Vibecoding） | `.showcase` 的 `grid-template-columns` | 默认 3fr/1fr（约原 1.2 倍）；想更宽改 3.5fr/1fr |
| 相机（俯视高度） | `camera.position.set`（ModelStage.astro） | y=2.0 俯视；y 越大越俯 |
| 模型位置/默认朝向 | `group.position.x` / `BASE_Y` | x=-0.35 左移量；BASE_Y=0.24 默认右侧微侧 |
| 鼠标视差幅度 | `tilt.ty = …*0.22` / `tilt.tx = …*0.1` | 0.22=水平角幅度、0.1=俯仰幅度；触屏自动关闭 |
| 三点布光 | `keyLight / fillLight / rimLight` | 主光左(-5,2.5,4) 2.2；辅光右(5,1.2,2.5) 0.55；轮廓光后上(0.5,5.5,-5.5) 2.6（penumbra 0.05=硬光） |
| ⚠️ 滚动动画失效排查 | `mixer.setTime` | 动画采样只由滚动 setTime 给定（LoopRepeat + 全程不调 mixer.update）。**别恢复 LoopOnce/clampWhenFinished**——播完后动作进"已完成"死态，setTime 无法唤醒（表现为滚动不再播动画）。采样点固定在末帧前 1ms（`clipDur - 0.001`）：LoopRepeat 在整段时长处取模回 0 帧，不收 ε 会导致滚过行程终点后模型跳回第 0 帧 |

## 四、出问题怎么办

- 动效错乱/内容飘走：`git checkout -- src/pages/index.astro` 回退到上次提交，再小步重来
- 改完全页白屏/报错：看终端里 `npm run dev` 的报错行号，多半是删补间时留了半个括号
- 深色底没升满内容就出现：把 `.scene-dark-bg` 的 duration 调小（升快些）或其后各 cue 调大
- 历次机制演变（v1→v4）与坑的来龙去脉：`docs/日志/2026-09-04-17-*` 与 README 决策表
